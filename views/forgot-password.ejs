<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Forgot Password</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
  <style>
    body {
      background-color: #f7fafc;
    }

    .forgot-password-container {
      background-color: #ffffff;
      padding: 1.5rem;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      max-width: 400px;
      width: 100%;
    }

    .forgot-password-container h2 {
      font-size: 1.5rem;
      font-weight: bold;
      margin-bottom: 1rem;
    }

    .info-message {
      font-size: 0.875rem;
      color: #4a5568;
      margin-bottom: 1rem;
    }

    .success-message {
      background-color: #d1fae5;
      border-l: 4px solid #38a169;
      color: #38a169;
      padding: 1rem;
      margin-bottom: 1rem;
    }

    .error-message {
      background-color: #fee2e2;
      border-l: 4px solid #e53e3e;
      color: #e53e3e;
      padding: 1rem;
      margin-bottom: 1rem;
    }

    .form-input {
      width: 100%;
      border: 1px solid #d2d6dc;
      border-radius: 4px;
      padding: 0.5rem;
      margin-bottom: 1rem;
      outline: none;
      transition: border-color 0.3s;
    }

    .form-input:focus {
      border-color: #4299e1;
    }

    .reset-button {
      width: 100%;
      background-color: #4a5568;
      color: #ffffff;
      font-weight: bold;
      padding: 0.75rem;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    .reset-button:hover {
      background-color: #2d3748;
    }
  </style>
</head>

<body class="flex items-center justify-center h-screen">
  <div class="forgot-password-container">
    <h2>Forgot Password</h2>
    <p class="info-message">Sistem kami akan mengirimkan email untuk mereset password anda.</p>
    <% if (status === "true") { %>
    <div class="success-message">
      <p><span class="font-semibold">Sukses:</span> <%= message %></p>
    </div>
    <% } else { %> <% if (message) { %>
    <div class="error-message">
      <p><span class="font-semibold">Error:</span> <%= message %></p>
    </div>
    <% } %> <% } %>
    <form action="/api/forgot-password" method="post">
      <div class="mb-4">
        <label for="email" class="block text-gray-600 font-semibold mb-2">Email</label>
        <input type="email" id="email" name="email" class="form-input" />
      </div>
      <button type="submit" class="reset-button">Reset Password</button>
    </form>
  </div>
</body>

</html>